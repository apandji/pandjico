{
  "name": "pandjico",
  "private": true,
  "scripts": {
    "build": "eleventy && node scripts/sync-built-projects.js",
    "serve": "eleventy --serve",
    "eleventy": "eleventy"
  },
  "devDependencies": {
    "@11ty/eleventy": "^2.0.1"
  }
}
